# --- PII: the real volunteer roster must NEVER be committed ---
*.xlsx
*.xls
*.csv
*.pdf
*.docx
!public/volunteer-orientation.pdf
out/
backup-*/
# contingent source docs + printed cluster directory (contact PII)
repistahanparadecontingentcheckinsoftware/
/260806 Cluster Directory*.png

# secrets
.env
.env.*
!.env.example

# build
node_modules/
dist/
dev-dist/
*.local

# editor/os
.DS_Store
*.code-workspace
*.tsbuildinfo

# test artifacts — never commit (playwright snapshots can contain roster PII and keys)
.playwright-mcp/
/pass-modal.png
/sample.png
